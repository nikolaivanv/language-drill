// Default session length — matches the 5 timeline items in design_handoff_language_drill/prototypes/web/hifi/dashboard.jsx.
export const DEFAULT_EXERCISE_COUNT = 5;

// A dictation-only run is short — a handful of clips, not a full 5-item drill.
export const DICTATION_RUN_COUNT = 4;
